# proxy runtime files
comfy_proxy.pid
comfy_proxy.log

# local untracked config (real ComfyUI IP, etc.)
comfy_proxy.env

# local v2 render workflow (kept untracked)
workflow2.json

# python venv for proxy vectorizer deps
.venv/

# SAM model checkpoints (large)
models/

# cached reference images for the before/after compare slider
.ideogrammar_refimg/

# gallery + saved-setups database (SQLite, incl. WAL sidecar files)
.ideogrammar.db
.ideogrammar.db-wal
.ideogrammar.db-shm

# python bytecode
__pycache__/
*.pyc
